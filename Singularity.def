Bootstrap: docker
From: ykhaliq2/flask-docker-app:latest

%post
    # Install additional dependencies if needed
    apt-get update && apt-get install -y curl

%environment
    export FLASK_ENV=production

%runscript
    # Start the Flask application
    exec python /app/app.py
